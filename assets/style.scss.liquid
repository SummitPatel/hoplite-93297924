/*============================================================================
  Fonts
==============================================================================*/

// font-family: 'PF DIN Text Pro Regular'
// font-family: 'PF DIN Text Pro Light'
// font-family: 'PF DIN Text Comp Pro Light'
// font-family: 'PF DIN Text Pro Bold'

/*============================================================================
  Dependency-free breakpoint mixin
    - http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
==============================================================================*/

$small: 640px;
$medium: 1099px;
$large: 1100px;
{% if settings.enable_wide_layout %}
$wide: 1250px;
{% endif %}

$viewportIncrement: 1px;
$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}


/*============================================================================
  Timber Overrides and Helper Classes
==============================================================================*/

.rte > div {
  margin-bottom: 0;
}

@include at-query($max, $small) {
  .padded {
    padding: 0;
  }
}

img {
  image-rendering: -moz-crisp-edges;         /* Firefox */
  image-rendering:   -o-crisp-edges;         /* Opera */
  image-rendering: -webkit-optimize-contrast;/* Webkit (non-standard naming) */
  image-rendering: crisp-edges;
  -ms-interpolation-mode: nearest-neighbor;  /* IE (non-standard property) */
}

/*============================================================================
  Site Nav & Header
==============================================================================*/

// hides the old header bar with the cart
.header-bar {
  padding: 0;
}

.site-header {
  position: absolute;
  z-index: 2;
  left: 0;
  right: 0;
  padding: 0;
  transition: all 1s ease;

  &.fixed-header {
    @include at-query($min, $postSmall) {
      position: fixed;
      background-color: #000;

      .wrapper {
        height: 60px;
        padding: 0 3vw;

        .grid--full {
          height: 60px;
        }

        .site-header__logo img {
          height: 35px;
        }

      }
    }
  }

  img {
    @include at-query($max, $small) {
      max-height: 10vw;
    }
  }

  .wrapper {
    padding: 3vh 3vw;
    max-width: 100%;
  }

  .cart-page-link {
    font-size: 1rem;
    padding-top: 8px;
    padding-bottom: 0;
  }
}

.site-nav__link {
  color: #fff;
  font-family: 'PF DIN Text Pro Bold';
  text-transform: uppercase;
  font-size: .8rem;
  padding-right: 35px;

  &:active, &:hover, &:focus {
    color: #fff;
  }

}

.icon-cart {
  color: #fff;
}

.mobile-nav-trigger {
  .icon {
    color: #fff;
    font-size: 7vw;
  }
}

.mobile-nav {
  text-align: center;
  height: 100vh;
  background: #000;
  opacity: .8;
  position: fixed;
  width: 100%;

  .x-btn {
    float: right;
    margin: 5vw 8vw;
    opacity: 1;
  }

  li {
    height: 12vh;
    border: none;

    &:first-of-type {
      margin-top: 20vh;
    }

    a {
      height: 12vh;
      color: #fff;
      opacity: 1;
      font-family: 'PF DIN Text Pro Bold';
      font-size: 7vw;
    }
  }
}


/*============================================================================
  Shared
==============================================================================*/

html {
  font-size: 14px;
}
body {
  font-family: 'PF DIN Text Pro Regular';
}

.grid {
  margin: 0;
}

.grid__item {
  padding: 0;
}

.frontpage-wrapper {
  margin: 0 auto;
  padding: 0;
  max-width: 100vw;
}

.centered {
  text-align: center;
  margin: 0 auto;
}

.centered-padded {
  text-align: center;
  margin: 0 auto;
  width: 100%;
  padding: 0;
}

.full-width {
  width: 100%;
}

.no-padding {
  padding: 0;
}

.tile-wrapper {
  position: relative;
  line-height: 1;
}

.content-wrapper {
  position: relative;
  overflow: auto;
  top: 50%;
  transform: translateY(-50%);
}

.text-wrapper {
  position: relative;
  color: #fff;
  text-transform: uppercase;
  text-align: center;

  h2, h4, h6 {
    font-family: 'PF DIN Text Pro Regular';
    margin: 0;
  }

  h2 {
    font-size: 6vw;
    font-family: 'PF DIN Text Comp Pro Light';
    letter-spacing: 1rem;
    @include at-query($max, $large) {
      font-size: 4rem;
      letter-spacing: .7rem;
    }
    @include at-query($max, $small) {
      font-size: 12vw;
      letter-spacing: .2rem;
    }
  }

  h4 {
    font-size: 2vw;
    img {
      max-height: 5vw;
    }
  }

  h6 {
    font-size: 1rem;
    font-family: 'PF DIN Text Pro Bold';
    letter-spacing: .1rem;
    @include at-query($max, $small) {
      font-size: 3.5vw;
    }
  }
}

.hero-tile {
  position: relative;
  height: 90vh;
  max-width: 100vw;
  background-size: cover;

  .hero-dropdown-wrapper {
    position: absolute;
    top: 91%;
    left: 0;
    right: 0;
    margin: 0 auto;
    color: #fff;
    cursor: pointer;

    p {
      margin-bottom: .5rem;
      font-family: 'PF DIN Text Pro Bold';
      font-size: 1rem;
      text-transform: uppercase;
      text-align: center;
      @include at-query($max, $small) {
        font-size: .8rem;
      }
    }
  }

  .hero-notch {
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 20px 40px 0 40px;
    border-color: #000000 transparent transparent transparent;
    text-align: center;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    margin: 0 auto;
    z-index: 1;
  }
}

.shop-now-btn {
  margin-top: 1rem;
  background: transparent no-repeat;
  border: 2px solid white;
  font-size: 1.2vw;
  letter-spacing: .1vw;
  font-family: 'PF DIN Text Pro Bold';
  text-transform: uppercase;
  line-height: 1;
  padding: 2vh 4vw;
  border-radius: 0;
  @include at-query($max, $small) {
    font-size: 4vw;
    padding: 4vh 10vw;
  }
}

.dropdown-chevron {
  margin: 0 auto;
  position: absolute;
  top: 95%;
  left: 0;
  right: 0;
}


/*============================================================================
  Homepage
==============================================================================*/

.hero-tile.hero-homepage {
  background-image: url( {{'hero-homepage-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'hero-homepage-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'hero-homepage-small.png' | asset_url}} );
  }
}

.worlds-toughest-workouts-tile, .meet-dyneema {
  height: 82vh;
  max-width: 100vw;
  background-size: cover;
  background-color: #e0d300;
  @include at-query($max, $large) {
    height: 66vh;
  }

  .text-wrapper {
    position: relative;
    color: #333;
    margin-bottom: 10vh;
    @include at-query($max, $small) {
     top: 18%;
    }

    h2, h6, p {
      margin: 0;
      font-family: 'PF DIN Text Pro Regular';
    }

    h2 {
      font-size: 5vw;
      font-family: 'PF DIN Text Comp Pro Light';
      letter-spacing: 1rem;
      @include at-query($max, $large) {
        font-size: 4rem;
        letter-spacing: .7rem;
      }
      @include at-query($max, $small) {
        font-size: 12vw;
        letter-spacing: .2rem;
      }
    }

    h6 {
      font-size: 1rem;
      letter-spacing: .1rem;
      @include at-query($min, $postSmall) {
        font-family: 'PF DIN Text Pro Bold';
      }
      @include at-query($max, $small) {
        font-size: 3vw;
      }
    }
  }
}

.icons-wrapper {
  position: relative;
  left: 0;
  right: 0;
  margin: auto;
  max-width: 85%;
  top: 50%;
  @include at-query(null, $postSmall, $large) {
    top: 60%;
  }
  @include at-query($max, $small) {
    top: 65%;
  }

  .icon {
    padding: 0;
  }

  img {
    margin-bottom: 5vh;
    @include at-query ($min, $postSmall) {
      min-height: 74px;
    }
    @include at-query($max, $small) {
      max-height: 10vw;
    }
  }

  p {
    text-transform: uppercase;
    font-size: 2vw;
    color: #333;
    line-height: 1.3;
    @include at-query(null, $postSmall, $large) {
      margin-top: 5vh;
    }
    @include at-query($max, $small) {
      font-size: 3vw;
    }
    @include at-query($max, $large) {
      font-size: 1rem;
    }
  }
}

.dyneema-tile { 
  background-size: cover;
  height: 82vh;
  @include at-query(null, $postSmall, $large) {
    height: 66vh;
  }
  @include at-query($max, $small) {
    height: 66vh;
  }

  .text-wrapper {
    max-width: 90vw;
    margin: 0 auto;

    h2, h4, h5 {
      font-family: 'PF DIN Text Pro Regular';
      margin: 0;
      padding: 0 0 1rem 0;
      line-height: 1;
    }

    h2 {
      font-family: 'PF DIN Text Pro Bold';
      letter-spacing: .4vw;
      font-size: 3vw;
      @include at-query($max, $large) {
        font-size: 3rem;
      }
      @include at-query($max, $small) {
        font-size: 7vw;
      }
    }

    h4 {
      font-family: 'PF DIN Text Pro Bold';
      letter-spacing: .2vw;
      font-size: 1.2vw;
      @include at-query($max, $large) {
        font-size: 1rem;
      }
      @include at-query($max, $small) {
        font-size: 3vw;
      }
      
    }

    h5 {
      font-size: 1.5vw;
      text-transform: none;
      @include at-query($max, $large) {
        font-size: 1.5rem;
      }
      @include at-query($max, $small) {
        font-size: 4vw;
      }

      .forced-line-break {
        display: none;
        @include at-query($max, $small) {
          display: block;
        }
      }
    }

    .dyneema-link {
      display: inline-block;
      background: transparent no-repeat;
      font-size: 1rem;
      font-family: 'PF DIN Text Pro Bold';
      text-transform: uppercase;
      letter-spacing: .1rem;
      line-height: 1;
      text-decoration: none;
      color: #fff;
      padding: .5rem 3rem;
      background-image: url( {{'dyneema-link-arrow.png' | asset_url}} );
      background-position: 90% 45%;
      @include at-query($max, $small) {
        font-size: .9rem;
        padding: .5vw 3vw;
        margin-top: 2vw;
        background-position: 100% 24%;
      }
    }

  }
}

.insane-abrasion-protection-tile {
  background-image: url( {{'insane-abrasion-protection-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'insane-abrasion-protection-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'insane-abrasion-protection-small.png' | asset_url}} );
  }
}

.rip-and-tear-proof-tile {
  background-image: url( {{'rip-and-tear-proof-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'rip-and-tear-proof-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'rip-and-tear-proof-small.png' | asset_url}} );
  }
}

.eerily-cold-tile {
  background-image: url( {{'eerily-cold-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'eerily-cold-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'eerily-cold-small.png' | asset_url}} );
  }
}

.perfectly-balanced-compression-tile {
  background-image: url( {{'perfectly-balanced-compression-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'perfectly-balanced-compression-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'perfectly-balanced-compression-small.png' | asset_url}} );
  }
}

.product-tile {
  .text-wrapper {
    margin: 0 auto;
    max-width: 80vw;
    @include at-query($min, $postSmall) {
      width: 33vw;
    }
  }

  .icon {
    margin-bottom: 2rem;
    max-height: 67px;
  }

  h2, h3, h4, h5, h6 {
    font-family: 'PF DIN Text Pro Regular';
    margin: 0;
  }

  h2 {
    font-size: 5rem;
    font-family: 'PF DIN Text Pro Light';
  }

  h3 {
    font-family: 'PF DIN Text Pro Bold';
    letter-spacing: .1rem;
    font-size: 3vw;
    @include at-query($max, $small) {
      font-size: 6vw;
      letter-spacing: .1rem;
    }
  }

  h4 {
    font-size: 2vw;
    font-family: 'PF DIN Text Pro Bold';
    letter-spacing: .3rem;
    @include at-query($max, $small) {
      font-size: 3.5vw;
      letter-spacing: .1rem;
    }
  }

  h5 {
    font-family: 'PF DIN Text Pro Bold';
    letter-spacing: .1rem;
    font-size: 1vw;
    @include at-query($max, $small) {
      font-size: 3vw;
    }
  }

  h6 {
    font-size: 1rem;
    line-height: 1.5;
    text-transform: none;
    @include at-query($max, $small) {
      font-size: 4vw;

      br {
        display: none;
      }
    }

  }
}

.designed-and-knit-tile, .buy-give-homepage-tile, .break-it-tile, .buy-give-tile {
  height: 48vh;
  background-size: cover;
  background: #333;
}

.designed-and-knit-tile.left-aligned, .break-it-tile {
  .text-wrapper {
    @include at-query($min, $postSmall) {
      margin-right: 10%;
    }
  }
}

.designed-and-knit-tile.right-aligned, .buy-give-homepage-tile, .buy-give-tile {
  .text-wrapper {
    @include at-query($min, $postSmall) {
      margin-left: 10%;
    }
  }
}

.knee-high-socks-homepage-tile, .arm-sleeves-tile, .leg-sleeves-homepage-tile, .add-compression, .add-dyneema {
  height: 48vh;
  background-size: cover;
}

.knee-high-socks-homepage-tile, .add-compression, .add-dyneema {
  background-image: url( {{'knee-high-socks-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'knee-high-socks-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'knee-high-socks-small.png' | asset_url}} );
  }
}

.arm-sleeves-tile {
  background-image: url( {{'arm-sleeves-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'arm-sleeves-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'arm-sleeves-small.png' | asset_url}} );
  }
}

.leg-sleeves-homepage-tile {
  background-image: url( {{'leg-sleeves-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'leg-sleeves-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'leg-sleeves-small.png' | asset_url}} );
  }
}


/*============================================================================
  Product Page
==============================================================================*/

.dyneema-link-black {
  display: inline-block;
  font-size: 1rem;
  font-family: 'PF DIN Text Pro Bold';
  color: #333;
  text-transform: uppercase;
  letter-spacing: .1rem;
  line-height: 1;
  text-decoration: none;
  padding: 1vw 2vw 1vw 0;
  background: transparent no-repeat;
  background-image: url( {{'link-arrow-black.png' | asset_url}} );
  background-position: 100% 45%;
  @include at-query($max, $small) {
    font-size: .9rem;
    padding: .5vw 3vw;
    margin-top: 2vw;
    background-position: 100% 24%;
  }
}

.link-arrow-black-reverse {
  position: absolute;
  display: inline-block;
  top: 3%;
  left: 3%;
  font-size: 1rem;
  font-family: 'PF DIN Text Pro Bold';
  color: #333;
  text-transform: uppercase;
  letter-spacing: .1rem;
  line-height: 1;
  text-decoration: none;
  padding: 1vw;
  background: transparent no-repeat;
  background-image: url( {{'link-arrow-black-reverse.png' | asset_url}} );
  background-position: 0 45%;
  @include at-query($max, $small) {
    font-size: .9rem;
    padding: .5vw 3vw;
    background-position: 0 24%;
  }
}

.hero-tile.hero-knee-high-socks {
  background-image: url( {{'hero-knee-high-socks-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'hero-knee-high-socks-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'hero-knee-high-socks-small.png' | asset_url}} );
  }
  
  .text-wrapper {
    color: #fff;
    text-transform: uppercase;

    br { display: none; }

    @include at-query($min, $postSmall) {
      text-align: left;
      float: right;

      br { display: block; }
    }

    h2 { line-height: 1; }

    h6 {
      font-size: 1.3vw;
      @include at-query($max, $small) {
        font-size: 3vw;
      }
    }

    a {
      color: #fff;
      text-decoration: underline;
    }
  }

  .hero-img-triangle-icon {
    p {
      @include at-query($max, $small) {
        font-size: 4vw;
      }
    }
  }
}

.indestructible-compression-tile {
  .text-wrapper {
    top: 20%;

    h2 {
      font-size: 4rem;
    }

    h6 {
      margin-bottom: 0;
      font-size: 1.2rem;
    }
  }

  .icons-wrapper {
    position: absolute;
    top: 60%;
    left: 0;
    right: 0;
    margin: auto;
    max-width: 80%;

    img {
      margin: 0 2rem;
    }
  }
}

.measurements-tile {
  height: 100vh;
  padding: 0 5vw;

  .text-wrapper {
    position: relative;
    color: #333;
    top: 0;
    padding: 5vw 0 0 0;

    h2 {
      font-family: 'PF DIN Text Pro Bold';
      font-size: 4vw;
      letter-spacing: .1rem;
      margin-bottom: 1vw;
      @include at-query($max, $small) {
        font-size: 8vw;
      }
    }

    h4 {
      font-family: 'PF DIN Text Pro Regular';
      font-size: 3vw;
      letter-spacing: .1rem;
      margin: 0;
      @include at-query($max, $small) {
        font-size: 5vw;
      }
    }

    h6 {
      font-family: 'PF DIN Text Pro Regular';
      font-size: 2vw;
      text-transform: none;
      margin: 0;
      @include at-query($max, $small) {
        display: none;
      }
    }
  }

  .text-container {
    height: 70vh;
    @include at-query($min, $postSmall) {
      padding: 0 0 0 10vw;
    }
    @include at-query($max, $small) {
      height: 30vh;
    }
  }

  ul {
    text-align: left;
    margin: 0 auto;
    position: relative;
    top: 50%;
    transform: translateY(-50%);

    h5 {
      font-family: 'PF DIN Text Pro Bold';
      font-size: 1.3vw;
      line-height: 1;
      margin: 0;
      letter-spacing: .1rem;
      text-transform: uppercase;
      @include at-query(null, $postSmall, $large) {
        font-size: 1.7vw;
      }
      @include at-query($max, $small) {
        font-size: 4vw;
      }
    }

    p {
      font-size: 1.3vw;
      font-family: 'PF DIN Text Pro Regular';
      letter-spacing: .05rem;
      text-transform: none;
      @include at-query(null, $postSmall, $large) {
        font-size:1.4vw;
      }
      @include at-query($max, $small) {
        font-size: 4vw;
      }
    }
  }

  .img-container {
    padding: 5vh 10vw 0 0;
    @include at-query($max, $small) {
      padding: 5vh 0 0 0;
    }

    img {
      width: 18vw;
      @include at-query(null, $postSmall, $large) {
        width: 25vw;
      }
      @include at-query($max, $small) {
        width: 50vw;
      }
    }
  }

  .icons-wrapper {
    font-family: 'PF DIN Text Pro Regular';
    margin: 5vw 0;

    .icon {
      height: 25vh;
      width: 25vh;
      font-family: 'PF DIN Text Pro Bold';
      font-size: 12vh;
      color: #989898;
      line-height: 25vh;
      border: 2px solid #999;
      border-radius: 18vh;
      margin: 0 auto;
    }

    .link-arrow {
      position: absolute;
      top: 10vh;
      right: 5vw;
    }

    .text {
      margin: 4vw 0 0 0;
    }

    h4 {
      text-transform: uppercase;
      font-family: 'PF DIN Text Pro Regular';
      letter-spacing: .1rem;
      color: #363636;
      margin: 0 0 2vw 0;
    }

    p {
      font-size: 3.8vw;
      text-transform: uppercase;
      margin: 0 0 4vw 0;
    }
  }

  .dropdown-chevron {
    @include at-query($max, $small) {
      display: none;
    }
  }
}

.measuring-tape-tile, .dyneema-four-wonders {
  background: #e9e9e9;
  height: 53vh;
  overflow: hidden;

  .icon {
    margin: 0 0 2rem 0;
  }

  .text-wrapper {
    color: #333;
    overflow: auto;
    margin: 0 10%;
  }

  .content-wrapper {
    margin: 0;
  }

  .text {
    margin: 0 auto;
    @include at-query($max, $small) {
      width: 65%;
    }
  }

  h4 {
    font-family: 'PF DIN Text Pro Regular';
    font-size: 3vw;
    letter-spacing: .1rem;
    margin: 0 0 5vh 0;
    @include at-query($max, $small) {
      font-size: 5vw;
      margin: 0 auto 5vh;
      max-width: 75%;
    }
  }

  h5 {
    margin: 0;
    padding: 0;
    font-family: 'PF DIN Text Pro Bold';
    font-size: 1.3vw;
    line-height: 1;
    letter-spacing: .1rem;
    text-transform: uppercase;
    @include at-query(null, $postSmall, $large) {
      font-size: 1.7vw;
    }
    @include at-query($max, $small) {
      font-size: .8rem;
    }
  }

  p {
    font-size: 1.1vw;
    letter-spacing: .05rem;
    text-transform: none;
    margin: 0;
    padding: 0 0 1rem 0;
    line-height: 1.5;
    @include at-query(null, $postSmall, $large) {
      font-size:1.4vw;
    }
    @include at-query($max, $small) {
      font-size: .8rem;
    }
  }
}

.measuring-tape-cover {
  background: #e9e9e9;
  position: absolute;
  height: 53vh;
  width: 100vw;
  top: 0;
  left: 0;

  .content-wrapper {
    margin: 0;
  }

  .text-wrapper {
    padding: 0;
  }

  a {
    color: #333;
    text-decoration: none;
  }
}

.product-single {
  position: relative;
  padding: 10vh 0;
  background: #333;

  .hero-triangle {
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 30px 60px 0 60px;
    border-color: #e9e9e9 transparent transparent transparent;
    text-align: center;
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    margin: 0 auto;
    z-index: 1;
  }
}

#ProductPhoto {
  width: 50vw;
  margin: 0 auto;
  float: none;
  text-align: center;
  padding: 0;
}

.product-single__hero {
  margin: 0;

  h1, .product-single__price {
    text-transform: uppercase;
    color: #fff;
    font-family: 'PF DIN Text Pro Bold';
    font-size: 1.5vw;
    line-height: 1;
    @include at-query($max, $small) {
      font-size: 6vw;
      text-align: center;
    }
  }

  h1 {
    margin: 0 0 1rem 0;
  }

  .product-single__price {
    @include at-query($min, $postSmall) {
      font-size: 3.5vw;
    }
  }
}

.product-details {
  text-align: left;
  padding: 0 5vw;
  @include at-query($max, $small) {
    text-align: center;
  }
}

.line-item-property__field {
  width: 50%;
  float: left;
  color: #fff;
  font-size: 1.3vw;
  text-transform: uppercase;
  @include at-query($max, $small) {
    font-size: 4vw;
  }

  p {
    margin-top: 30px;
    margin-left: 15px;
  }

  label {
    text-align: left;
    width: 90%;
  }

  select {
    width: 90%;
    background-color: #333;
    background-image: url('dropdown-arrow.png');
    border-color: #999;
    color: #fff;
    text-transform: uppercase;
    font-size: 1.3vw;
    @include at-query($max, $small) {
      font-size: 4vw;
    }
  }

  &:last-of-type {
    margin: 0 0 4vh 0;
    width: 90%;
    text-align: center;

    &:after {
      content: "";
      display: table;
      clear: both;
    }

    .radio-btn-label {
      width: 50%;
      float: left;
      cursor: pointer;

      /* HTML5 Boilerplate accessible hidden styles */
      [type="radio"] {
        border: 0; 
        clip: rect(0 0 0 0); 
        height: 1px; margin: -1px; 
        overflow: hidden; 
        padding: 0; 
        position: absolute; 
        width: 1px;
      }

      /* the basic, unchecked style */
      [type="radio"] + span:before {
        content: '';
        display: inline-block;
        width: 2rem;
        height: 2rem;
        vertical-align: -0.5rem;
        border-radius: 1rem;
        border: 0.150rem solid #fff;
        margin-right: 0.75rem;
        transition: 0.25s ease all;
        @include at-query($max, $small) {
          width: 1.5rem;
          height: 1.5rem;
        }
      }

      /* the checked style using the :checked pseudo class */
      [type="radio"]:checked + span:before {
        background: #fff;
        box-shadow: 0 0 0 .09rem #fff;
        border: 0.45rem solid #333;
      }
    }

    input {
      margin: 0 3vw 0 0;
    }

    span {
      margin: 0 4vw 0 2px;
      text-transform: uppercase;
      font-family: 'PF DIN Text Pro Bold';
    }
  }


}

#AddToCart {
  background-color: #fff;
  color: #4d4d4d;
  border-radius: 0px;
  text-transform: uppercase;
  font-family: 'PF DIN Text Pro Bold';
  width: 60%;
  @include at-query($max, $small) {
    width: 90%;
  }
}

#AddToCartForm {
  overflow: auto;

  hr {
    margin: 5vh auto;
    border-color: #5c5c5c;
  }
}

.workout-tile {
  height: 5rem;
  line-height: 4.5;
}


/*============================================================================
  Cart.liquid
==============================================================================*/

.empty-cart-content {
  margin: 10rem 0;
}


/*============================================================================
  Collection.liquid
==============================================================================*/

.section-header.section-header--breadcrumb {
  margin-top: 10rem;
}


/*============================================================================
  Tabbed Content
==============================================================================*/

.tab-container {
  position: relative;
  padding-top: 5vh;

  .tabs {
    margin: 0 auto;
    padding: 0;
    width: 20vw;
    @include at-query($max, $small) {
      width: 80vw;
    }

    &:before, &:after {
      content: " ";
      display: block;
      height: 0;
      overflow: hidden;
      visibility: hidden;
      width: 0;
      clear: both;
    }

    li {
      display: block;
      float: left;
      height: 2rem;
      margin: 0 auto;
      padding: 0;
      width: 10vw;
      @include at-query($max, $small) {
        width: 40vw;
      }

      &.flex-active {
        border-bottom: 1px solid red;
      }

      a {
        display: block;
        font-size: 13px;
        height: 29px;
        line-height: 30px;
        margin: 0;
        padding: 0;
        text-transform: uppercase;
        width: auto;
        color: #303030;
        border-bottom: none !important;

        &:active {
          background: none repeat scroll 0 0 #FFFFFF;
          border-left-width: 1px;
          border-top-left-radius: 2px;
          border-top-right-radius: 2px;
          color: red;
          height: 30px;
          margin: 0 0 0 -1px;
          padding-top: 4px;
          position: relative;
          top: -4px;
        }
      }
    }
  }

  #tab-1, #tab-2 {

  }

  .medical-grade-compression-tile, .medical-grade-fit-tile {
    overflow: auto;
    padding: 0 8vw;
  }

  .img-container {
    padding: 5vh 0;

    img {
      width: 25vw;
      @include at-query(null, $postSmall, $large) {
        width: 33vw;
      }
      @include at-query($max, $small) {
        width: 33vw;
      }
    }
  }

  .tabbed-content-text-wrapper {
    position: relative;
    padding: 0;
    height: 90vh;
    @include at-query(null, $postSmall, $large) {
      padding: 0 0 0 10vw;
    }
    @include at-query($max, $small) {
      height: 40vh;
    }

    h1 {
      font-family: 'PF DIN Text Pro Bold';
      font-size: 3vw;
      color: #c1c1c2;
      text-transform: uppercase;
      position: relative;
      top: 50%;
      transform: translateY(-50%);
    }

    ul {
      text-align: left;
      margin: 0 auto;
      position: relative;
      top: 50%;
      transform: translateY(-50%);

      h5 {
        font-family: 'PF DIN Text Pro Bold';
        font-size: 1.3vw;
        line-height: 1;
        margin: 0;
        letter-spacing: .1rem;
        text-transform: uppercase;
        @include at-query(null, $postSmall, $large) {
          font-size: 1.7vw;
        }
        @include at-query($max, $small) {
          font-size: 4vw;
        }
      }

      p {
        font-size: 1.3vw;
        font-family: 'PF DIN Text Pro Regular';
        letter-spacing: .05rem;
        @include at-query(null, $postSmall, $large) {
          font-size:1.4vw;
        }
        @include at-query($max, $small) {
          font-size: 4vw;
        }
      }
    }
  }

  .dropdown-chevron {
    @include at-query($max, $small) {
      display: none;
    }
  }
}

.compression-sweet-spot-tile {
  background: #e9e9e9;
  height: 41vh;
  @include at-query($max, $small) {
    height: 31vh;

    h2 { font-size: 7vw; }

    h5 { font-size: 4vw; }
  }

  .text-wrapper {
    color: #333;
  }

  a {
      color: #333;
      text-decoration: underline;
    }
}

.better-than-jeans-protection-tile, .wont-wear-out-tile {
  height: 82vh;

  @include at-query($max, $small) {
    height: 53vh;

    .text-wrapper {
      width: 85vw;

      br { display: none; }

      h2 { font-size: 7vw; }

      h5 { display: none; }
    }
  }
}

.better-than-jeans-protection-tile {
  background-image: url( {{'better-than-jeans-protection-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'better-than-jeans-protection-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'better-than-jeans-protection-small.png' | asset_url}} );
  }
}

.wont-wear-out-tile {
  background-image: url( {{'wont-wear-out-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'wont-wear-out-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'wont-wear-out-small.png' | asset_url}} );
  }
}

.no-show-socks-tile, .leg-sleeves-tile {
  height: 48vh;
  background-size: cover;
}

.no-show-socks-tile {
  background-image: url( {{'no-show-socks-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'no-show-socks-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'no-show-socks-small.png' | asset_url}} );
  }
}

.leg-sleeves-tile {
  background-image: url( {{'leg-sleeves-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'leg-sleeves-medium.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'leg-sleeves-small.png' | asset_url}} );
  }
}

/*============================================================================
  Color Swatches
==============================================================================*/

{% assign width = '47px' %}
{% assign height = '45px' %}

.swatches-container {
  display: table;
  margin: 0;

  @include at-query($max, $small) {
    margin: 0 auto;
  }
}

.swatch-element {
  display: inline-block;
  -webkit-transform:translateZ(0); /* webkit flicker fix */
  -webkit-font-smoothing:antialiased; /* webkit text rendering fix */
  /* Spacing between buttons */
  margin:0px 10px 10px 0;
  /* To position the sold out graphic and tooltip */
  position:relative;
}

.selected-swatch-color {
  font-family: 'PF DIN Text Pro Bold';
  color: #fff;
  text-transform: uppercase;
}

/* Hide radio buttons.*/
.swatch input { 
  display:none;
}
.swatch label {
  /* Rounded corners */
  -webkit-border-radius:2px;
  -moz-border-radius:2px;
  //border-radius:2px;
  /* To give width and height */
  float:left;
  /* Color swatches contain no text so they need to have a width. */
  min-width:{{ width }}; 
  height:{{ height }};
  /* No extra spacing between them */
  margin:0;
  /* The border when the button is not selected */
  //border:#ccc 1px solid;
  /* Background color */
  background-color:#ddd;
  /* Styling text */
  font-size:13px;
  text-align:center;
  line-height:{{ height }};
  white-space:nowrap;
  text-transform:uppercase;
  background: no-repeat;
}
.swatch-element label { padding:0 10px; }
.color.swatch-element label {
  padding:0;
  background-position: 5px 5px;
  }
/* Styling selected swatch */
.swatch input:checked + label {
  &:after {
    content: '';
    position: absolute;
    width: 47px;
    height: 50px;
    left: 0;
    background: url( {{ 'color-swatch-border.png' | asset_url }} ) no-repeat;
  }
} 
/* Image with the cross in it */
.crossed-out { position:absolute; width:100%; height:100%; left:0; top:0; }
.swatch .swatch-element .crossed-out { display:none; }
.swatch .swatch-element.soldout .crossed-out { display:block; }
.swatch .swatch-element.soldout label {
  filter: alpha(opacity=30); /* internet explorer */
  -khtml-opacity: 0.3;      /* khtml, old safari */
  -moz-opacity: 0.3;       /* mozilla, netscape */
  opacity: 0.3;           /* fx, safari, opera */
}
/* CSS triangle */
.swatch .tooltip:after {
  border-left:solid transparent 10px;
  border-right:solid transparent 10px;
  border-top:solid gray 10px;
  bottom:-10px;
  content:" ";
  height:0;
  left:50%;
  margin-left:-13px;
  position:absolute;
  width:0;
}
.swatch .swatch-element:hover .tooltip {
  filter:alpha(opacity=100);
  -khtml-opacity:1;
  -moz-opacity:1;
  opacity:1;
  visibility:visible;
  -webkit-transform:translateY(0px);
     -moz-transform:translateY(0px);
      -ms-transform:translateY(0px);
       -o-transform:translateY(0px);
          transform:translateY(0px);
}
.swatch.error {
  background-color:#E8D2D2!important;
  color:#333!important;
  padding:1em;
  border-radius:5px;
}
.swatch.error p {
  margin:0.7em 0;
}
.swatch.error p:first-child {
  margin-top:0;
}
.swatch.error p:last-child {
  margin-bottom:0;
}
.swatch.error code {
  font-family:monospace;
}


/*============================================================================
  Product Landing Page (Collections Page)
==============================================================================*/
.hero-tile.hero-product-landing-page {
  background-image: url( {{'hero-product-landing-page.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'hero-product-landing-page.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'hero-product-landing-page.png' | asset_url}} );
  }
}


/*============================================================================
  Compression 101 Page
==============================================================================*/

.hero-tile.hero-compression-101 {
  background-image: url( {{'hero-compression-101-large.png' | asset_url}} );
  @include at-query(null, $postSmall, $large) {
    background-image: url( {{'hero-product-landing-page.png' | asset_url}} );
  }
  @include at-query($max, $small) {
    background-image: url( {{'hero-compression-101-small.png' | asset_url}} );
  }

  .text-wrapper {
    width: 75%;
    margin: 0 auto;
  }
}

.compression-101 {
  height: 74vh;
  @include at-query($max, $small) {
    height: 94vw;
  }

  .icon {
    margin: 0 0 2rem 0;
    @include at-query($max, $small) {
      width: 40px;
      margin: 0 0 1rem 0;
    }
  }

  .text-wrapper {
    width: 85%;
    margin: 0 auto;
  }

  .dropdown-chevron {
    top: 93%;
    @include at-query($max, $small) {
      width: 12.5px;
      display: none;
    }
  }
}

.blood-flow, .measurement {
  background-color: #d05d00;
}

.tighter, .result {
  background-color: #333;
}

.slider {
  margin: 0 auto;
  @include at-query($max, $small) {
    width: 75%;
    padding: 2rem 0 0 0;
  }
}

.infographic {
  .grid__item {
    @include at-query($max, $small) {
      float: none;
    }
  }

  .slides {
    margin: 0;
  }

  .slider-content {
    margin: 0 auto;
  }

  li {
    position: relative;
    margin: 1.5rem 0 0 0;
  }

  .prev, .next {
    position: absolute;
    top: 27%;
    cursor: pointer;
    width: 32px;
    height: 32px;
    @include at-query($min, $postSmall) {
      display: none;
    }
  }

  .prev {
    left: 5%;
  }

  .next {
    right: 5%;
  }

  .prev-icon, .next-icon {
    position: relative;
    top: 50%;
    transform: translateY(-50%);
  }

  .text-wrapper {
    padding: 1rem;
    text-align: left;
    @include at-query($min, $postSmall) {
      top: 50%;
      transform: translateY(-50%);
    }
  }

  h3 {
    margin: 0 0 .25rem 0;
    padding: 0 1rem;
    color: #333;
    font-family: 'PF DIN Text Pro Bold';
    font-size: 1.3rem;
  }

  p {
    margin: 0 0 1rem 0;
    padding: 0 1rem;
    color: #333;
    font-size: .9rem;
    text-transform: none;
    line-height: 1.5;
  }

  span {
    font-weight: bold;
  }
}

.diamond-infographic {
  overflow: auto;

  .slider {
    @include at-query($max, $small) {
      padding: 2rem 0 0 0;
      width: 75%;
    }
  }

  .slides {
    position: relative;
    @include at-query($min, $postSmall) {
      display: none;
    }
  }

  .prev, .next {
    top: 17%;
  }

  .orange {
    color: #d05d00;
    text-transform: uppercase;
  }

  .text-wrapper {
    text-align: center;
    @include at-query($min, $postSmall) {
      position: absolute;
      left: 63vw;
      text-align: left;
      width: 25%;
    }
  }
}

.diamond-infographic-img {
  margin: 4vw;
  width: 50vw;
}

.goldilocks-infographic, .familiar-infographic {
  background: #e9e9e9;
}

.goldilocks-infographic {
  overflow: auto;
  padding: 0 5%;

  .text-wrapper {
    color: #333;
    text-align: center;
    @include at-query($min, $postSmall) {
      position: absolute;
      width: 25%;
      text-align: left;
    }
  }

  @include at-query($min, $postSmall) {
    .slides {
      margin: 5% 0;
    }

    // Look in html for Timber helper classes that change the order visually but not in code
    li:first-child {
      margin-top: 0%; 
    }
    li:nth-child(2n + 0) {
      margin-top: 20%;
    }
    li:last-child {
      margin-top: 40%;
    }
  }

  .text {
    text-align: center;
    transform: none;
    position: relative;
    @include at-query($min, $postSmall) {
      width: inherit;
    }

    h6 + h6 {
      font-family: 'PF DIN Text Pro Regular';
    }
  }
}

.too-loose-circle, .just-right-circle, .too-tight-circle, .inner-circle-1, .inner-circle-2, .inner-circle-3 {
  width: 18vw;
  height: 18vw;
  border: 1px solid black;
  border-radius: 50%;
  position: relative;
  margin: 0 auto;
  @include at-query($max, $small) {
    width: 65vw;
    height: 65vw;
  }
}

.inner-circle-1, .inner-circle-2, .inner-circle-3 {
  top: 50%;
  transform: translateY(-50%);
}

.circle-text {
  position: relative;
  top: 55%;
  transform: translateY(-50%);
  text-transform: uppercase;

  h3 {
    font-size: 4vw;
    @include at-query($min, $postSmall) {
      font-size: 1.3vw;
    }
  }

  p {
    font-size: 3.5vw;
    @include at-query($min, $postSmall) {
      font-size: 1.2vw;
    }
  }

  img {
    width: 33%;
  }
}

.too-loose-circle {
  .inner-circle-1 {
    width: 16vw;
    height: 16vw;
    @include at-query($max, $small) {
      width: 57vw;
      height: 57vw;
    }
  }
}

.just-right-circle {
  .inner-circle-1 {
    width: 16vw;
    height: 16vw;
    @include at-query($max, $small) {
      width: 57vw;
      height: 57vw;
    }
  }
  .inner-circle-2 {
    width: 14vw;
    height: 14vw;
    @include at-query($max, $small) {
      width: 48vw;
      height: 48vw;
    }
  }
}

.too-tight-circle {
  .inner-circle-1 {
    width: 16vw;
    height: 16vw;
    @include at-query($max, $small) {
      width: 57vw;
      height: 57vw;
    }
  }
  .inner-circle-2 {
    width: 14vw;
    height: 14vw;
    @include at-query($max, $small) {
      width: 48vw;
      height: 48vw;
    }
  }
  .inner-circle-3 {
    width: 12vw;
    height: 12vw;
    @include at-query($max, $small) {
      width: 40vw;
      height: 40vw;
    }
  }
}

.magic-numbers-infographic {
  .content-wrapper {
    top: 0;
    transform: none;
  }

  img {
    width: 33vw;
    @include at-query($min, $postSmall) {
      width: 20vw;
    }
    margin: 8vw 0;
  }

  .text-wrapper {
    padding: 1rem;
    text-align: center;
    @include at-query($min, $postSmall) {
      position: absolute;
      left: 50%;
      width: 25%;
      margin: 0 0 0 2vw;
      text-align: left;
    }
  }

  h3 {
    @include at-query($min, $postSmall) {
      letter-spacing: .1rem;
    }
  }
}

.familiar-infographic {
  height: 53vh;
  @include at-query($min, $postSmall) {
    height: 96vh;
    padding: 0 5%;
  }

  .text-wrapper {
    padding: 1rem;
    text-align: center;
    @include at-query($min, $postSmall) {
      width: 25%;
      text-align: left;
    }
  }

  .slider-content {
    color: #333;
    text-align: center;
    padding: 0;
    top: 0;
    transform: none;
    width: 80%;
    @include at-query($min, $postSmall) {
      text-align: left;
    }
  }

  .text {
    @include at-query($min, $postSmall) {
      width: inherit;
    }
  }

  h2 {
    font-size: 12vw;
    font-family: 'PF DIN Text Comp Pro Light';
    text-transform: uppercase;
    line-height: 1;
    @include at-query($min, $postSmall) {
      font-size: 4vw;
    }
  }
}

.familiar-slider {
  margin: 0 auto 2rem;
  @include at-query($min, $postSmall) {
    position: relative;
    top: 50%;
    transform: translateY(-50%);
  }
}



/*============================================================================
  Dyneema Page
==============================================================================*/

.hero-dyneema {
  background-color: #000;
}

.meet-dyneema {
  background-color: #1a4880;
  @include at-query($max, $large) {
    height: 55vh;
  }

  .text-wrapper {
    color: #fff;
  }

  h6 {
    text-transform: none;
  }

  img {
    margin-bottom: 1vh;
  }

  p {
    color: #fff;
    @include at-query($max, $small) {
      font-size: 4vw;
    }
  }
}

.made-with-dyneema {
  .grid__item + .grid__item {
    height: 35vw;
    @include at-query($max, $small) {
      height: 26vh;
    }
  }

  .bg-img {
    background-image: url({{ 'made-with-dyneema-graphic.png' | asset_url }});
    background-size: cover;
    height: 70vh;
    @include at-query($max, $small) {
      background-image: url({{ 'made-with-dyneema-graphic-small.png' | asset_url }});
      height: 26vh;
    }
  }

  .content-wrapper {
    @include at-query($min, $postSmall) {
      width: 50%;
      margin: 0 auto;
    }
  }
}

.dyneema-rip {
  background-color: #333;
}

.dyneema-us-them {
  height: inherit;

  h2 {
    padding: 0;
  }
}

.dyneema-us, .dyneema-them {
  background-size: cover;
  height: 26vh;
  @include at-query($min, $postSmall) {
    height: 70vh;
  }
}

.dyneema-us {
  @include at-query($min, $postSmall) {
    background-image: url({{ 'dyneema-us-large.png' | asset_url }});
  }
  @include at-query($max, $small) {
    background-image: url({{ 'dyneema-us-small.png' | asset_url }});
  }
}

.dyneema-them {
  @include at-query($min, $postSmall) {
    background-image: url({{ 'dyneema-them-large.png' | asset_url }});
  }
  @include at-query($max, $small) {
    background-image: url({{ 'dyneema-them-small.png' | asset_url }});
  }
}

.dyneema-why-bother {
  background-color: #e9e9e9;
  height: 41vh;
  @include at-query($max, $small) {
    height: 31vh;
  }

  .content-wrapper {
    margin: 0 auto;
    @include at-query($min, $postSmall) {
      width: 66%;
    }
  }

  .text-wrapper {
    color: #333;

    h5 {
      line-height: 1.3;
    }
  }
}

.add-dyneema {
  .text-wrapper {
    @include at-query($min, $postSmall) {
      width: 42vw;
    }
  }
}

.dyneema-four-wonders {
  height: inherit;
  background: #fff;

  .text-wrapper {
    margin: 0 auto;
  }

  .number {
    color: #989898;
    border: 1px solid #989898;
    border-radius: 50%;
    height: 6vh;
    width: 6vh;
    line-height: 6vh;
    margin: 0 auto;
  }

  h2 {
    margin: 10% auto 5%;
    font-family: 'PF DIN Text Pro Bold';
    letter-spacing: .4vw;
    font-size: 3vw;
    @include at-query($max, $large) {
      font-size: 3rem;
    }
    @include at-query($max, $small) {
      font-size: 7vw;
    }
  }

  h5 {
    text-align: left;
  }

  p {
    text-align: left;
    padding-bottom: 2rem;
  }
}

